{
  "name": "tab-death",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/copy-static.mjs",
    "typecheck": "tsc --noEmit",
    "sanity": "npm run typecheck && npm run build"
  },
  "dependencies": {
    "dexie": "^4.0.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5",
    "vite": "^5.4.8"
  }
}
