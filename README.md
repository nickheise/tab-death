# tab death 💀
Capturing your good intentions right before they rot in a browser graveyard of 47 tabs you'll never revisit.

## Local development

### Install dependencies

```
npm install
```

### Build the extension

```
npm run build
```

This outputs a `dist/` folder. Load that folder via `chrome://extensions` → "Load unpacked".

### Typecheck

```
npm run typecheck
```
